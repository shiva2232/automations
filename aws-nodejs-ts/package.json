{
  "name": "my-project",
  "version": "1.0.0",
  "description": "This is a sample package.json file",
  "main": "index.js",
  "scripts": {
    "start:dev": "serverless offline --aws-profile alm-shopify-pos",
    "lint": "eslint --cache src --ext .ts",
    "lint:fix": "npm run lint -- --fix",
    "prettier-format": "prettier --config .prettierrc src//.ts --write",
    "prepare": "husky install"
  },
  "workspaces": [
    "packages/*"
  ],
  "author": "John Doe",
  "license": "MIT",
  "dependencies": {
    "express": "^4.17.1",
    "lodash": "^4.17.21"
  },
  "devDependencies": {
    "eslint": "^7.32.0",
    "husky": "^9.0.11",
    "prettier": "^2.8.8",
    "serverless-offline": "^13.3.3"
  }
}
